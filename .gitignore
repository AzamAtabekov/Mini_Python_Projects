# Mini_Python_Projects .gitignore

# --- Python bytecode / caches
__pycache__/
*.py[cod]
*$py.class

# --- Virtual environments
# Common venv names — не коммитим
.env
.env.*
.venv/
venv/
ENV/
env/
env.bak/
.pdm-python
.pixi/
__pypackages__/

# --- OS files
.DS_Store
Thumbs.db
desktop.ini

# --- Editors / IDEs
.vscode/
.idea/
*.sublime-workspace
*.sublime-project
*.code-workspace

# --- Logs / temporary
*.log
*.tmp
*.temp
tmp/
temp/

# --- Packaging / build
build/
dist/
*.egg-info/
*.egg
pip-wheel-metadata/
wheels/
*.whl
.Python

# --- Test / coverage
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.pytest_cache/
.cache
nosetests.xml
coverage.xml

# --- Jupyter notebooks
.ipynb_checkpoints

# --- Database / local data
*.sqlite3
*.db
*.db-journal
*.sql

# --- PyInstaller / deploy artifacts
*.manifest
*.spec

# --- Certificates / keys / secrets (very important)
# common env / secret files
.env
.env.local
.env.*.local
secrets/
private_files/
*.env
*.secret
*.key
*.pem
*.pfx
*.jks
*.crt
*.cer

# SSH private keys (do NOT ignore .pub)
id_rsa
id_ecdsa
id_ed25519
id_dsa

# cloud provider credentials
.aws/
.aws/credentials
.aws/config
.gcloud/
gcloud/
google_credentials.json
azureauth.json
credentials.json

# Git credentials / netrc
.netrc
.git-credentials
credential.helper

# macOS Keychain / keyrings
.keyring
*.kdbx

# --- Containers / Docker (ignore envs & volumes)
docker-compose.override.yml
docker-compose.*.env
*.env.*.local
docker-compose.env
.vagrant/
.vagrant.d/

# --- Node / frontend artifacts (if present)
node_modules/
npm-debug.log
yarn-error.log

# --- Images/large media (optional — uncomment if you don't want to keep media in repo)
# *.png
# *.jpg
# *.jpeg
# *.gif
# images/tmp/

# --- Misc
*.bak
*.swp
*.swo
*.old

# ===========================
# Notes:
# - If you DO want to keep a configuration sample, commit e.g. `.env.example` (without secrets).
# - If a legitimate file is accidentally ignored, you can force-add it: git add -f path/to/file
# ===========================
